name: Lint

on: # yamllint disable-line rule:truthy
  push:
    branches:
     - develop
  workflow_dispatch:

#permissions: {}

jobs:
  build:
    name: Lint
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: read
      # To report GitHub Actions status checks
      statuses: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # super-linter needs the full git history to get the
          # list of files that changed across commits
          fetch-depth: 0

      - name: Super-linter
        uses: super-linter/super-linter/slim@v7.2.1 # x-release-please-version
        env:
          # To report GitHub Actions status checks
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_ALL_CODEBASE: true
          DISABLE_ERRORS: true
          VALIDATE_JAVA: true
          VALIDATE_GOOGLE_JAVA_FORMAT: true
          LOG_LEVEL: DEBUG
          #FIX_GOOGLE_JAVA_FORMAT: true
      - name: Upload GOOGLE_JAVA_FORMAT Stdout
        uses: actions/upload-artifact@v4
        with:
          name: super-linter-parallel-stdout-GOOGLE_JAVA_FORMAT
          path: /tmp/super-linter-output/super-linter-parallel-stdout-GOOGLE_JAVA_FORMAT
